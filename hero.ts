import { heroui } from "@heroui/theme";

export default heroui({
  themes: {
    light: {
      colors: {
        foreground: {
          DEFAULT: "#1e1a4d",
        },
        default: { foreground: "#1e1a4d" },
        content1: "#fdf7f4",
      },
    },
  },
});
